{
  "name": "laundry-app",
  "version": "1.0.0",
  "private": true,
  "description": "Interactive step-by-step guide to the family laundry process",
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix web install",
    "dev:server": "npm --prefix server run dev",
    "dev:web": "npm --prefix web run dev",
    "test": "npm --prefix server test",
    "build:web": "npm --prefix web run build",
    "typecheck": "npm --prefix server run typecheck && npm --prefix web run typecheck",
    "gen-vapid": "npm --prefix server run gen-vapid"
  }
}
