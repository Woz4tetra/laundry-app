services:
  laundry:
    build: .
    container_name: laundry-app
    restart: unless-stopped
    env_file: .env
    environment:
      - DATA_DIR=/data
      - PORT=${PORT:-23103}
    ports:
      - "${PORT:-23103}:${PORT:-23103}"
    volumes:
      - ./data:/data
